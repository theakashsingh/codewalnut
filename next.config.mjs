// /** @type {import('next').NextConfig} */
// const nextConfig = {
    
// };

// export default nextConfig;


/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: false,  // Enabling React Strict Mode
   
    swcMinify: true,  // Enabling SWC minification for faster builds
  };
  
  export default nextConfig;